export default function useGetAcademicFacultyQuery() {
  const handleDeleteAcademicFaculty = async (id: string) => {
    try {
      // Delete Logic
    } catch (error) {}
  };

  return {
    handleDeleteAcademicFaculty,
  };
}
